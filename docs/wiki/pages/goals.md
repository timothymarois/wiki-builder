+++
title = "Goals"
subtitle = "every approved page's intent, in sidebar order"
status = "approved"
goals = false
intent = """
This page collects what each part of the tool is for, so that the whole purpose can be read in one
sitting and a change to any of it is visible in one place.
"""
+++

The goals page collects the intent of every approved page, except a page that says `goals = false`, when
the wiki is built, in the order the sidebar lists the pages.
Changing an intent changes what that part of the tool is trying to be, so the change needs approval.
